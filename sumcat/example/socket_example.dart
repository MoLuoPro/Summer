import 'package:sumcat/sumcat.dart';

void main() {
  // var app = createApplication();
  // app.tcp('/test', [
  //   (socket) => {
  //         socket.listen((event) {
  //           print(event);
  //         })
  //       }
  // ]);
  // app.listen(4000);
}
